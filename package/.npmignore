# Published package contents are controlled by the `files` allowlist in package.json.
# Keep this file so npm-compatible packers do not fall back to .gitignore, which
# intentionally ignores generated Bullet sources required by the native package.
